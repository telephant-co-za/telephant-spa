import React from 'react';
import Typography from '@material-ui/core/Typography'

export default function LogoText({}) {
    return(
        <>
            <Typography>Telephant</Typography>
            <Typography>Cloud</Typography>
        </>
    )
}